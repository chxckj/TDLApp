/**
 * @format
 */

import {AppRegistry} from 'react-native';
import 'react-native-gesture-handler';
import App from './App';

// AppRegistry.registerComponent("정신차려", () => App);
AppRegistry.registerComponent("TDLApp", () => App);
